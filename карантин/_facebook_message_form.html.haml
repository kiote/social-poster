
= form_for(@facebook_message) do |f|
  = render 'shared/error_messages', object: f.object
  %div.field
    = f.text_area :text, placeholder: "compose new facebook message", class: "textarea"
  = f.submit "Post", class: "btn btn-large btn-primary"
